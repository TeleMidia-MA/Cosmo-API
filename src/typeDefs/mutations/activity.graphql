# import Activity from "../types/activity.graphql"

input ActivityInput {
    title: String!
}

type Mutation {
    createActivity(activity: ActivityInput) : Activity @hasRole(requires: administrator)
}